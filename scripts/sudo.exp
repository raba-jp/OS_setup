#!/usr/bin/expect

set command [lindex $argv 0]
set password [lindex $argv 1]

set timeout 5

spawn bash
send "sudo ${command}\r"

expect {
	-glob "Password:" {
		send -- "${password}\n"
	}
}

expect {
	-glob "$" {
		exit 0
	}
}
